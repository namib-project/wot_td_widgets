name: wot_td_widgets
description: Exprimental Widgets for UI Elements generated from WoT Thing Descriptions.
version: 0.0.1
homepage: https://github.com/namib-project/wot_td_widgets

environment:
  sdk: '>=2.18.5 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  dart_wot: ^0.22.0
  flutter_json_schema_form: ^0.0.10
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
